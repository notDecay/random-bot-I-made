export namespace Embed {
  /**This is just a shorthand of this code
   * ```ts
   * new Date().toISOString()
   * ```
   * @returns a Date
   */
  export function createNewDate() {
    return new Date().toISOString()
  }
}